# Local Testing Environment Configuration
# Copy this file to .env.test and update with your local values

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mgscale_dev
DB_USER=postgres
DB_PASSWORD=your_password_here

# Redis Cache
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# EventHub (dummy values for local testing)
EVENT_HUB_CONNECTION_STRING=Endpoint=sb://dummy.servicebus.windows.net/
EVENT_HUB_NAME=dummy-eventhub

# Optional: Alert API
# ALERT_API_BASE_URL=http://localhost:8000
